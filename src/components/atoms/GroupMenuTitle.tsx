import React from 'react'

const GroupMenuTitle = ( {title}: {title: string} ) => {
  return (
    <h3 className=' uppercase font-black text-font text-xs '> {title} </h3>
  )
}

export default GroupMenuTitle